name: Publish Docker image

on:
  push:
    tags: ["v*"]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    env:
      REGISTRY: ${{ secrets.DOCKER_REGISTRY || 'ghcr.io' }}
      USERNAME: ${{ secrets.DOCKER_USER || github.actor }}
      PASSWORD: ${{ secrets.DOCKER_TOKEN || secrets.GITHUB_TOKEN }}

    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Login to registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ env.USERNAME }}
          password: ${{ env.PASSWORD }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/c-sellers/graphed-knowledge:latest
            ${{ env.REGISTRY }}/c-sellers/graphed-knowledge:${{ github.ref_name }}
